# NIP Insight

```
cd NipInsight
docker-compose -f docker-compose.yml build
docker-compose -f docker-compose.yml up -d

API: http://localhost:5020/nip/0000000000?date=2023-07-04
frontend: http://localhost:3000/
```